mode: Mainline
branches:
  main:
    regex: ^main$
    increment: Patch
    prevent-increment-of-merged-branch-version: true
  master:
    regex: ^master$
    increment: Patch
    prevent-increment-of-merged-branch-version: true
ignore:
  sha: []
